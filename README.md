# LazyCopy

LazyCopy is a Windows-first capture companion for Codex workflows. It creates a local screenshot artifact and manifest that can be attached to the latest Codex session through supported CLI image input.
